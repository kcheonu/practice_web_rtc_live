import React from "react";

function RestoValue({ track, participantIdentity, local = false, onCaptureFrame }) {
    return(
    <div>가게 이름, 평점, 등급(가게 전체 평가 점수 공개)</div>
    );
}

export default RestoValue;